/**
 * FLUXO CONDICIONAL
 * Utilização de IF, ELSE
 * @author Ricardo Vieira
 */

public class ControleFluxo2 {
    /* Método principal da classe */
    public static void main(String[] args) {
        int numero = 8; //define o valor da variavel em 8
        if ((numero % 2) == 0) { //verifica se o resto da divisão é igual a 0
            System.out.println ("Este é um número par");//Imprime em tela a mensagem
        }   else {
                System.out.println ("Esse é um número Impar"); //Imprime em tela a mensagem
        }//fim do método
    }//fimm do método
}//fim da classe